[[services]]
  internal_port = 5173
  name = "frontend"
  command = "npm run dev"
  dockerfile = """
    FROM node:16
    WORKDIR /src/app
    COPY . .
    RUN npm install
    EXPOSE 5173
    CMD [ "npm","run", "dev" ]
  """
